const NotFound: React.FC = () => {
  return (
    <main className="not-found">
      <h1>Not found</h1>
      <p>Unfortunately, we could not find the requested page or restource.</p>
    </main>
  );
};

export default NotFound;
